{
  "name": "font-generator",
  "version": "0.0.1",
  "description": "Generates cross-browser CSS for web fonts",
  "main": "src/index.js",
  "bin": {
    "font-generator": "./bin/generator"
  },
  "scripts": {
    "test": "npm run lint && npm run build",
    "prebuild": "rimraf dist && mkdir dist",
    "build": "node ./bin/generator > ./dist/fonts.css && cpy ./assets/* ./dist/assets && npm run uglify",
    "publish:cdn": "frau-publisher -m lib -t fonts -f './dist/**' -k AKIAJ74TXZZMW4IVWJOA --secretvar S3_SECRET --versionvar TRAVIS_TAG",
    "lint": "eslint src",
    "uglify": "uglify -s ./dist/fonts.css -o ./dist/fonts.min.css -c"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Brightspace/font-generator.git"
  },
  "keywords": [
    "font",
    "generator"
  ],
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/Brightspace/font-generator/issues"
  },
  "homepage": "https://github.com/Brightspace/font-generator",
  "devDependencies": {
    "cpy": "^3.4.1",
    "eslint": "^1.7.1",
    "eslint-config-brightspace": "0.0.2",
    "eslint-plugin-react": "^3.6.2",
    "frau-publisher": "^2.5.1",
    "rimraf": "^2.4.3",
    "uglify": "^0.1.5"
  }
}
